<form method="post" action="{{ path('compteclient_delete', {'idcompteclient': compteclient.idcompteclient}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compteclient.idcompteclient) }}">
    <button class="btn btn-filters active">Delete</button>
</form>

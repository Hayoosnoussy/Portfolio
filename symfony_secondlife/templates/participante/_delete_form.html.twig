<form method="post" action="{{ path('participante_delete', {'idParticipant': participante.idParticipant}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participante.idParticipant) }}">
    <button class="btn">Delete</button>
</form>

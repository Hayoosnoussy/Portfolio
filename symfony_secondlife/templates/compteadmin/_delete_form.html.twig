<form method="post" action="{{ path('compteadmin_delete', {'idcompteadmin': compteadmin.idcompteadmin}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compteadmin.idcompteadmin) }}">
    <button class="btn btn-filters active">Delete</button>
</form>

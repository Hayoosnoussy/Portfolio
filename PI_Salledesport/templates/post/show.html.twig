{% extends 'base_front_forum.html.twig' %}

{% block title %}Publication{% endblock %}

{% block body %}
    <h1>Publication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ post.sujet }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ post.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ post.date ? post.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('post_index') }}" class="btn btn-success">Retour à la liste</a>

    <a href="{{ path('post_edit', {'id': post.id}) }}" class="btn btn-primary">Modifier</a>

    {{ include('post/_delete_form.html.twig') }}
{% endblock %}

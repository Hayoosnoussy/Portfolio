{{ form_start(form) }}
    <table>
        <tr>
            <td>Sujet</td>
            <td>{{ form_widget(form.sujet) }}</td>
        </tr>
        <tr>
            <td>Description</td>
            <td>{{ form_widget(form.description) }}</td>
        </tr>
        <tr>
            <td>Categorie</td>
            <td>{{ form_widget(form.categorie) }}</td>
        </tr>
        <tr>
            <td></td>
            <td><button class="btn">{{ button_label|default('Publier') }}</button></td>
        </tr>
    </table>

{{ form_end(form) }}

{% extends 'base_front.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ commentaire.id }}</td>
        </tr>
        <tr>
            <th>Date</th>
            <td>{{ commentaire.date ? commentaire.date|date('Y-m-d') : '' }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ commentaire.description }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('commentaire_index') }}" class="btn btn-success">Retour à la liste</a>

    <a href="{{ path('commentaire_edit', {'id': commentaire.id}) }}" class="btn btn-primary">Modifer</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
